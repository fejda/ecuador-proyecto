services:
  app:
    build: .
    container_name: ecuador-app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - JWT_SECRET=clave-super-secreta-para-produccion-2025
    volumes:
      - h2-data:/app/data
    restart: unless-stopped

volumes:
  h2-data: